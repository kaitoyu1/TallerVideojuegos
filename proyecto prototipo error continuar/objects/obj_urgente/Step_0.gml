/// @description Inserte aquí la descripción
// Puede escribir su código en este editor

// Evento Step
if (being_dragged) {
    // Mueve el objeto junto con el mouse manteniendo el offset
    x = mouse_x - drag_offset_x;
    y = mouse_y - drag_offset_y;
}
